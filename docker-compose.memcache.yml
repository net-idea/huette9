services:
  memcached:
    image: memcached
    container_name: '${APP_NAME:-huette9}_memcached'
    hostname: memcached
    restart: unless-stopped
    environment:
      MEMCACHED_MEMORY_LIMIT: 128
    networks:
      - internal

networks:
  internal:
