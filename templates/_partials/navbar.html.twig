<nav class="navbar navbar-expand-lg navbar-glass shadow-sm sticky-top">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center fw-bold" href="{{ path('app_home') }}">
      <span class="brand-icon me-2">
        🏡
      </span>
      <span class="brand-text">Hütte9</span>
    </a>
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-lg-center">
        <li class="nav-item"><a class="nav-link px-3 {{ app.request.get('_route') == 'app_home' ? 'active' : '' }}" href="{{ path('app_home') }}">{{ 'nav.home'|trans }}</a></li>
        <li class="nav-item"><a class="nav-link px-3 {{ app.request.get('_route') == 'app_booking' ? 'active' : '' }}" href="{{ path('app_booking') }}">{{ 'nav.booking'|trans }}</a></li>
        <li class="nav-item"><a class="nav-link px-3 {{ app.request.get('_route') == 'app_contact' ? 'active' : '' }}" href="{{ path('app_contact') }}">{{ 'nav.contact'|trans }}</a></li>

        {# Language switcher #}
        <li class="nav-item ms-lg-2 navbar-lang-switcher">
          <div class="dropdown">
            <button
              class="btn btn-lang-toggle dropdown-toggle d-flex align-items-center"
              type="button"
              data-bs-toggle="dropdown"
              aria-expanded="false"
              aria-label="Sprache auswählen"
            >
              <span class="lang-icon">🌐</span>
              <span class="lang-code">{{ app.request.locale|upper }}</span>
            </button>

            <ul class="dropdown-menu dropdown-menu-end shadow-lg border-0 navbar-lang-menu">
              <li>
                <a
                  class="dropdown-item navbar-lang-item d-flex align-items-center {{ app.request.locale starts with 'de' ? 'is-active' : '' }}"
                  href="{{ path('app_locale_switch', { locale: 'de' }) }}"
                >
                  <span class="navbar-lang-flag">🇩🇪</span>
                  <span class="navbar-lang-text">
                    <span class="navbar-lang-name">{{ 'language.german'|trans }}</span>
                    <span class="navbar-lang-label">DE</span>
                  </span>
                </a>
              </li>
              <li>
                <a
                  class="dropdown-item navbar-lang-item d-flex align-items-center {{ app.request.locale starts with 'en' ? 'is-active' : '' }}"
                  href="{{ path('app_locale_switch', { locale: 'en' }) }}"
                >
                  <span class="navbar-lang-flag">🇬🇧</span>
                  <span class="navbar-lang-text">
                    <span class="navbar-lang-name">{{ 'language.english'|trans }}</span>
                    <span class="navbar-lang-label">EN</span>
                  </span>
                </a>
              </li>
            </ul>
          </div>
        </li>

        {# Theme switcher #}
        <li class="nav-item ms-lg-2 navbar-theme-switcher">
          <div class="dropdown">
            <button
              class="btn btn-theme-toggle dropdown-toggle d-flex align-items-center"
              type="button"
              data-bs-toggle="dropdown"
              aria-expanded="false"
              aria-label="Theme auswählen"
            >
              <span class="theme-icon">🌙</span>
              <span class="theme-label">Theme</span>
            </button>

            <ul class="dropdown-menu dropdown-menu-end shadow-lg border-0 navbar-theme-menu">
              <li>
                <button class="dropdown-item navbar-theme-item d-flex align-items-center" data-theme="system">
                  <span class="navbar-theme-icon">💻</span>
                  <span class="navbar-theme-text">
                    <span class="navbar-theme-name">{{ 'theme.system'|trans }}</span>
                    <span class="navbar-theme-label">Auto</span>
                  </span>
                </button>
              </li>
              <li>
                <button class="dropdown-item navbar-theme-item d-flex align-items-center" data-theme="light">
                  <span class="navbar-theme-icon">☀️</span>
                  <span class="navbar-theme-text">
                    <span class="navbar-theme-name">{{ 'theme.light'|trans }}</span>
                    <span class="navbar-theme-label">Light</span>
                  </span>
                </button>
              </li>
              <li>
                <button class="dropdown-item navbar-theme-item d-flex align-items-center" data-theme="dark">
                  <span class="navbar-theme-icon">🌙</span>
                  <span class="navbar-theme-text">
                    <span class="navbar-theme-name">{{ 'theme.dark'|trans }}</span>
                    <span class="navbar-theme-label">Dark</span>
                  </span>
                </button>
              </li>
            </ul>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>
